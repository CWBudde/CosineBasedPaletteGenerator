# CosineBasedPaletteGenerator

A generator based on differential evolution for cosine based palettes. With a given png image containing a color gradient, the optimal cosine based code will be computed.
